{
  "name": "taco",
  "private": true,
  "scripts": {
    "build": "esbuild action.ts --bundle --format=cjs --platform=node --legal-comments=none --outfile=action.js && deno fmt action.ts --no-semicolons --single-quote"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "indent-string": "^5.0.0",
    "semver": "^7.5.4"
  },
  "devDependencies": {
    "@types/node": "^20.4.7",
    "@types/semver": "^7.5.0",
    "esbuild": "^0.18.19",
    "typescript": "^5.1.6"
  }
}
